name: Deploy Release
run-name: ${{ github.actor }} triggered deploy to ${{ github.event.inputs.env }} pipeline for tag version ${{ github.event.inputs.tag_version }}

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Select environment to deploy'
        required: true
        default: 'preprod'
        type: choice
        options:
          - preprod
          - prod
      tag_version:
        description: 'Tag version to deploy'
        required: true
        default: ''

jobs:
  cloud-db-backup:
    if: ${{ github.event.inputs.env == 'prod' }}
    uses: HDRUK/github-actions-templates/.github/workflows/cloud-db-backup.yaml@main
    with:
      env: ${{ github.event.inputs.env }}
      tag_version: ${{ github.event.inputs.tag_version }}
    secrets: inherit

  deploy:
    uses: HDRUK/github-actions-templates/.github/workflows/deploy-release.yml@main
    with:
      env: ${{ github.event.inputs.env }}
      tag_version: ${{ github.event.inputs.tag_version }}
      chart_path: chart/gateway-api/Chart.yaml
      git_ref: main
      run_scan: true
    secrets: inherit